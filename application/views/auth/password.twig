{% extends "layouts/auth.twig" %}

{% block title %}Forgot password{% endblock %}

{% block content %}
<div class="login-box">
    <div class="login-logo">
        {{ config.item('app_name') }}
    </div>
    <div class="login-box-body">
        <p class="login-box-msg">Enter your email address</p>
        {{ form_open() }}
            <div class="form-group has-feedback{{ form_error('email') ? ' has-error'}}">
                <input type="email" name="email" class="form-control" placeholder="Email">
                <span class="glyphicon glyphicon-envelope form-control-feedback"></span>
                {{ form_error('email') }}
            </div>
            <div class="row">
                <div class="col-xs-12">
                    <button type="submit" class="btn btn-primary btn-block btn-flat" name="send" value="true">Send</button>
                </div>
            </div>
        {{ form_close() }}
        <a href="{{ site_url('login') }}">Back to login page</a><br>
    </div>
</div>
{% endblock %}
{% block scripts %}
{{ parent() }}
<script>
    $(function () {
        $('input').iCheck({
            checkboxClass: 'icheckbox_square-blue',
            radioClass: 'iradio_square-blue',
            increaseArea: '20%' /* optional */
        });
    });
</script>
{% endblock %}
